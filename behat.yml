default:
  paths:
    features: %behat.paths.base%/app/tests/features/api
    bootstrap: %behat.paths.base%/app/tests/features/bootstrap
  context:
    parameters:
        base_url: http://riiingme.app
        guzzle: true
  extensions:
    Behat\MinkExtension\Extension:
      goutte: ~
      base_url: http://riiingme.app
